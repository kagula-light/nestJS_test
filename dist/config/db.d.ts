declare const config: {
    mysql: {
        port: number;
        host: string;
        user: string;
        password: string;
        database: string;
        connectionLimit: number;
    };
};
export default config;
